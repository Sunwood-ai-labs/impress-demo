# draw-io-skill Slide

`draw-io-skill` v0.1.0 を紹介する `impress.js` スライドです。

## 使い方

リポジトリのルートでサーバーを起動します。

```bash
npm install
npm start
```

ブラウザで `http://127.0.0.1:4173/slide/draw-io-skill/` を開くとスライドを確認できます。

## 操作

- `Space`
- `←` / `→`
- `Tab`
